# TranslateGemma CLI 长文本翻译快速参考

## 🚀 快速开始

```bash
# 激活环境
cd ~/Code/GenAI/translategemma-cli
source .venv/bin/activate

# 翻译长文本（自动分块）
translate --file long_article.txt

# 自定义分块
translate --file book.txt --chunk-size 80 --overlap 20

# 流式输出
translate --file article.txt --stream

# 批量翻译
translate --dir ./documents
```

---

## 📖 参数速查

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--chunk-size` | 100 | 每块大小（字符） |
| `--overlap` | 30 | 重叠大小（字符） |
| `--no-chunk` | false | 禁用分块 |
| `--stream` | false | 流式输出 |
| `--dir` | - | 批量翻译目录 |

---

## 🎯 推荐配置

### 日常使用
```bash
--chunk-size 100 --overlap 30
```

### 书籍翻译
```bash
--chunk-size 80 --overlap 20
```

### 快速翻译
```bash
--chunk-size 120 --overlap 20
```

---

## ⚡ 性能参考

| 文本长度 | 耗时 | 吞吐量 |
|----------|------|--------|
| 100字符 | 1.2秒 | 83字符/秒 |
| 400字符 | 8.5秒 | 48字符/秒 |
| 1000字符 | ~22秒 | ~45字符/秒 |

---

## 💡 最佳实践

1. **文本 < 300字符**: 使用 `--no-chunk`
2. **文本 300-1000字符**: 使用默认配置
3. **文本 > 1000字符**: `--chunk-size 80 --overlap 20`
4. **需要实时反馈**: 添加 `--stream`
5. **批量处理**: 使用 `--dir`

---

## 🐛 常见问题

**Q: 翻译不完整？**  
A: 减小 chunk_size 到 60-80

**Q: 翻译有重复？**  
A: 减小 overlap 到 15-20

**Q: 翻译太慢？**  
A: 增大 chunk_size 到 120-150

**Q: 如何翻译整本书？**  
A: `translate --file book.txt --chunk-size 80 --overlap 20 --stream`

---

*快速参考 v1.0 | 2026-01-17*
